services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: spring-api:latest
    container_name: spring-api
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod